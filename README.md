CodingTest1:-
int this first i take unorderset for the finding the frquency of the string and then in loop 1 i put the n==s and and find the frquency by frq[n]++ it iterate the frquency of the every character then in secound loop if in this frq==1 then return that character or print ch 
and this condition is not setisfy then print -1



Coding test 2:-
First, I take n as the size of the array and then take all n elements as input.Then I calculate the actual sum of all the elements present in the array by iterating through the array.After that, I calculate the expected sum of all numbers from 1 to n+1 using the sum formula.Then I subtract the actual sum from the expected sum.The number left after subtraction is the missing number, so I print that number.
